import transport.Transport;
import transport.energyType.InternalCombustionEngine;
import transport.movingEnvironment.waterTransport.WaterTransport;
import transport.purpose.PublicTransport;

public class Ferry extends Transport implements WaterTransport, InternalCombustionEngine, PublicTransport {
}
