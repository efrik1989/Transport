import transport.Transport;
import transport.energyType.SteamEngine;
import transport.movingEnvironment.groundTransport.Railway;
import transport.purpose.PublicTransport;

public class Locomotive extends Transport implements SteamEngine, PublicTransport, Railway {
}
