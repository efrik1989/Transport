import transport.Transport;
import transport.energyType.SteamEngine;
import transport.movingEnvironment.waterTransport.WaterTransport;
import transport.purpose.PublicTransport;

public class Steamship extends Transport implements SteamEngine, WaterTransport, PublicTransport {
}
