import transport.Transport;
import transport.energyType.ElectricEngine;
import transport.movingEnvironment.groundTransport.OffStreetTransport;
import transport.purpose.PublicTransport;

public class Subway extends Transport implements ElectricEngine, PublicTransport, OffStreetTransport {
}
